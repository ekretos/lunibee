# Lunibee benchmarks

Run benchmarks with Bun after installing dependencies.

The benchmark suite is intentionally kept outside `packages/` so benchmark code never becomes part of the runtime library.
